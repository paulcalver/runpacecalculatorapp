import SwiftUI
import UIKit

extension Color {
    static let appBackground = Color(hue: 120.0/360.0, saturation: 0.6, brightness: 0.6)
}

extension UIColor {
    static let appBackground = UIColor(hue: CGFloat(120.0/360.0), saturation: 0.6, brightness: 0.6, alpha: 1.0)
}
